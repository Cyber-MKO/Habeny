# Python >= 3.10
#
# The LXC Python bindings are not listed here: install them from the OS
# package manager (they must match the host's liblxc):
#   sudo apt install python3-lxc
fastapi==0.141.1
uvicorn==0.53.0
websockets==17.1          # WebSocket support for uvicorn (live metrics, container console)
pydantic==2.13.5
cryptography==50.0.1      # encryption of stored SIEM auth keys
PyJWT==2.15.0             # verifies single sign-on (OIDC) ID tokens
reportlab==4.4.9          # PDF reports
matplotlib==3.10.8        # report charts
