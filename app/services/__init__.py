"""
Service layer — business logic called by route handlers.

Each module in this package owns a cohesive slice of domain logic.
Routes never access core/installers/db directly; they go through services.
"""
