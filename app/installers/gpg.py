"""
Atomicorp GPG key management for OSSEC installation.

NOTE: The Atomicorp installer (atomic_installer.sh) handles GPG key
setup internally.  Manual GPG key management was removed after it
proved unreliable across GPG 2.x keybox format changes.

This module is reserved for any future GPG key utilities if needed.
"""
